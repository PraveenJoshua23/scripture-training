# ElevenLabs Tamil TTS pipeline (scripts/tts/). Copy to .env and fill in.
# .env is gitignored — never commit real keys.
ELEVENLABS_API_KEY=
ELEVENLABS_VOICE_ID=

# Only needed for scripts/tts/upload_to_cdn.py
CDN_BUCKET=
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=auto
CDN_ENDPOINT_URL=
CDN_PUBLIC_BASE_URL=
